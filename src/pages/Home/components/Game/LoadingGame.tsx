import React, {FC} from 'react';
import styles from "./game.module.css"
const LoadingGame: FC = () => {
    return (
        <div className={styles.loadingGame}>
            
        </div>
    );
};

export default LoadingGame;